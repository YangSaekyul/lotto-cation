# 로또 당첨 판매점 지도 — 모바일 웹 기획서

> **For Hermes:** Build only after the data-location pipeline is proven; the existing legacy lotto repository is data-only input and must not be reused as application code.

**서비스명:** 로또리 (LottoRi)

**한 줄 설명:** 가까운 로또 판매점과 과거 당첨 이력을 쉽게 찾는 모바일 웹

**프로젝트 주소:** `미배포` — 데이터 최신화와 지도 MVP 검증 후 Vercel production 주소를 확정한다.

**Git 저장소:** https://github.com/YangSaekyul/lotto-cation

**로컬 프로젝트:** `/Users/yang/CODE/lotto-cation`

**기존 프로젝트 상태:** `YangSaekyul/lotto`는 코드 재사용 없이 폐기 대상으로 둔다. 원천·추출 데이터는 이 신규 저장소의 `data/`에 보존하며, 기존 저장소를 삭제/아카이브하기 전 보안 자격증명 노출 여부를 처리한다.

**Goal:** 30대 이상 사용자가 휴대폰에서 가까운 로또 판매점을 찾고, 과거 당첨 이력과 직전 회차 결과를 쉽고 과장 없이 확인하게 하는 모바일 웹 서비스.

**Architecture:** Next.js PWA + Naver Maps JavaScript API + server-side geocoding/normalization job + PostgreSQL/Supabase. 당첨 이력은 지도/검색용 정적·정규화 데이터로 제공하고, 회차 결과와 판매점 상태는 공식 원천 갱신 이력으로 분리한다.

**First release target:** 모바일 360–480px 우선, 태블릿 768px까지 최적화. 데스크톱은 기능 접근만 보장하며 별도 고밀도 UI를 만들지 않는다.

---

## 1. 제품 판단

### MVP 범위 판정: 조건부 승인

아래 기능이면 MVP로 충분하다. 단, 지도 서비스의 본질은 **가까운 판매점 탐색**이지 ‘명당 예측’이 아니다.

| 기능 | MVP 포함 | 이유 |
|---|---:|---|
| 전체 당첨 판매점 지도 | O | 서비스의 첫 화면/핵심 가치 |
| 내 주변 1~5등 이력 강조 | O | 탐색 우선순위를 빠르게 제공 |
| 직전 회차 번호·등수별 당첨자 수 | O | 재방문 동기 |
| 길찾기 연결 | O | 지도 서비스의 행동 전환 |
| 폐점·오류 제보 | O | 데이터 유지 비용을 사용자와 분산 |
| 기간별 당첨번호 통계 | O | 반복 방문용 보조 기능 |
| 최다 당첨 이력 탭 | O | 탐색 콘텐츠 |
| 데이터 기반 번호 추천 + 1회 후원 | **보류 후 제한 출시** | 로또 당첨 예측으로 오인될 위험, 결제/정책 검토 필요 |

### 하지 않을 것

- ‘명당’, ‘확률 상승’, ‘당첨 보장’, ‘AI가 다음 번호를 맞힌다’ 같은 표현
- 판매점 과거 이력을 미래 당첨 가능성으로 연결하는 랭킹
- 회원가입, 커뮤니티, 매주 자동결제, 푸시 알림은 v1 제외
- 데스크톱 전용 대시보드

### 필수 고지

모든 판매점 상세·추천 화면에 다음을 같은 톤으로 표시한다.

> 과거 당첨 이력과 번호 통계는 향후 당첨 확률을 높이지 않습니다. 번호 추천은 오락·기록 목적의 무작위 조합입니다.

후원은 ‘번호 구매’가 아니라 서비스 운영 후원이어야 하며, 실제 출시 전 결제사업자 정책·전자상거래/사행성 표시 요건을 확인한다. **1,000원 결제와 번호 추천을 직접 1:1로 묶는 모델은 정책 검토 전에는 공개하지 않는다.**

---

## 2. 사용자와 핵심 시나리오

### 1차 사용자

- 30~50대, 로또를 가끔 구매하거나 퇴근길에 판매점을 찾는 사용자
- 지도 앱은 쓰지만 당첨 이력·최근 회차 정보를 한 화면에서 보고 싶은 사용자
- 복잡한 차트보다 ‘가까운 곳’, ‘길찾기’, ‘지난 회차 결과’를 빠르게 원하는 사용자

### 20초 핵심 동선

1. 사이트 진입 → 위치 권한 선택
2. 지도에서 현재 위치·주변 판매점 핀 확인
3. `가까운 판매점` 목록에서 거리/1등·2등 이력 확인
4. 한 판매점 선택 → 네이버 지도 길찾기

### 재방문 동선

1. 토요일 추첨 뒤 직전 회차 번호 확인
2. 당첨자 수·1등 배출 판매점 확인
3. `1개월 / 3개월 / 6개월 / 1년 / 5년` 번호 통계에서 자주 나온 번호를 탐색

---

## 3. 모바일 UX와 디자인 방향

### 디자인 원칙

- **밝은 바탕 + 짙은 글자 + 한 가지 초록 행동색.** 금융앱처럼 신뢰감을 주되, 네이버·카카오 지도 UI를 모방하지 않는다.
- 장식보다 판독성: 30대 이상 기준 본문 16px 이상, 핵심 숫자 20–28px, 최소 터치영역 48×48px.
- 다크 모드·글래스모피즘·작은 아이콘 남발 제외.
- 한 화면에는 주 행동 하나만: `내 주변 보기`, `길찾기`, `최근 회차 보기`.

### 토큰

| 항목 | 값 |
|---|---|
| 배경 | `#F7F8F5` (따뜻한 백색) |
| 본문 | `#17211C` |
| 행동색 | `#0F8A5F` |
| 강조 이력 | 1등 `#E54B4B`, 2등 `#F59E0B`, 3등 `#3B82F6`, 4·5등 `#6B7280` |
| 카드 | 흰색, 16px radius, 얇은 테두리만 |
| 폰트 | Pretendard 또는 system Korean sans-serif |
| 기본 간격 | 8px 단위, 화면 좌우 16px |

### 반응형 기준

| 너비 | 레이아웃 |
|---|---|
| 360–479px | 지도 전체 화면 + 하단 바텀시트, 1열 카드 |
| 480–767px | 지도 + 넓어진 바텀시트, 2열 통계 카드 가능 |
| 768–1024px | 태블릿: 지도 60% / 목록 40% 분할 |
| 1024px 이상 | 모바일 레이아웃을 최대 960px 컨테이너로 유지; PC 전용 기능 추가 금지 |

### 홈 화면 구조

```text
[ 로또 어디서 살까                         ]
[ 현재 위치로 찾기 ] [ 회차 결과 ]
-------------------------------------------
|                                           |
|             네이버 지도                   |
|  ● 일반 판매점   ● 1등 이력  ● 2등 이력   |
|                                           |
-------------------------------------------
[ 내 주변 당첨 판매점 12곳              v ]  ← 드래그 바텀시트
[ 1등 3회  스파                  420m   ]
[ 2등 12회 로또마트              680m   ]
[ 5등 이력 편의점               1.1km   ]
[    위치 새로고침 ] [ 필터 ]
[ 지도 ] [ 최근 결과 ] [ 번호 통계 ] [ 최다 판매점 ]
```

### 지도 핀 규칙

- 전체 판매점: 작은 중립 핀, 낮은 줌에서는 클러스터로 묶는다.
- 1등 이력: 큰 빨간 원형 배지 + `1등 n회`; 가장 먼저 보인다.
- 2등 이력: 주황 배지 + `2등 n회`.
- 3등: 파란 배지. 4·5등은 지도에서 일반 핀과 같은 크기로 두고 상세에서만 표시한다.
- **‘당첨점’ 대신 ‘과거 1등/2등 당첨 이력 판매점’**을 기본 표기로 사용한다.
- `인터넷 복권판매사이트`는 오프라인 지도에서 제외한다.

### 판매점 상세 바텀시트

1. 상호명·주소·거리
2. `네이버 지도 길찾기` 큰 초록 버튼
3. 과거 당첨 이력: 1등/2등/3등/4등/5등 숫자
4. 최근 당첨 회차 목록(최대 5개)
5. `이 지점 정보가 틀렸나요? 제보하기`
6. 고지 문구

---

## 4. 화면 목록

| 화면 | 목적 | 핵심 구성 |
|---|---|---|
| 지도 홈 | 근처 판매점 찾기 | 위치 권한, 지도, 등수 필터, 바텀시트 목록 |
| 판매점 상세 | 길찾기 전환 | 주소, 거리, 이력, 제보 버튼 |
| 직전 회차 | 추첨 직후 재방문 | 회차, 6개 번호+보너스, 1~5등 당첨자 수, 1등 판매점 |
| 번호 통계 | 기간별 재미 요소 | 기간 탭, 번호 1~45 빈도 그리드, ‘확률과 무관’ 고지 |
| 최다 당첨 이력 | 탐색 콘텐츠 | 등수별 정렬/지역 필터, 지도 열기 |
| 오류 제보 | 데이터 유지 | 폐점/이전/주소오류/등수오류, 사진은 v1 제외 |
| 후원/번호 추천 | 보류 기능 | 후원 취지, 면책, 무작위 조합 결과, 재발급 금지 |

---

## 5. 데이터 설계

### 보유 원천

현재 데이터 팩: `/Users/yang/CODE/lotto-cation-data/`

- `winning_store_history.csv`: 당첨 이력 51,456건
- `winning_stores.json`: 상호·주소 합산 14,079건
- 모든 좌표가 비어 있음 → 지도 MVP의 선행 차단 요소

### 핵심 테이블

```text
stores
- id, name, normalized_name, address, normalized_address
- latitude, longitude, geocode_provider, geocode_status, geocoded_at
- is_closed, reported_status, updated_at

winning_store_records
- id, draw_no, prize_rank, store_id, raw_name, raw_address, source_url, collected_at

draw_results
- draw_no, draw_date, winning_numbers[6], bonus_number
- prize_rank, winners_count, prize_amount, source_url, collected_at

store_reports
- id, store_id, type(closed/moved/address_error/other), detail, status, created_at
```

### 데이터 파이프라인

1. legacy CSV/JSON import
2. 상호·주소 공백/도로명/번지 정규화
3. `name + normalized_address` 기준으로 판매점 병합
4. Naver geocoding API로 좌표 조회
5. 실패·모호 주소는 `geocode_status=review_required`로 저장하고 지도에서 제외
6. 거리 검색용 spatial index 생성
7. 동행복권 공식 결과를 회차별로 수집·검증
8. 지점 제보는 바로 공개 반영하지 않고 `pending`으로 보관

### 운영 기준

- 데이터 기준시각을 지도와 회차 화면에 표시
- 주소가 없는 지점, 지오코딩 신뢰도 낮은 지점은 ‘확인 필요’로 표시
- 지점 제보가 2건 이상 또는 공식 원천으로 확인된 경우에만 상태 변경
- 당첨 이력 갱신은 매주 추첨 후 1회 + 실패 시 재시도

---

## 6. 기능 상세 및 수용 기준

### A. 근처 지도

- 위치 권한 거부 시: `서울시청` 등 기본 중심이 아니라, 검색창으로 지역을 직접 입력하게 한다.
- 내 위치에서 5km 안을 기본 검색하되 1/3/5/10km 필터 제공.
- 판매점 목록은 거리 오름차순, 동률이면 1등→2등 이력 순.
- 수용 기준: 위치가 있으면 3초 이내 지도와 가까운 20개 목록을 함께 표시한다.

### B. 네이버 길찾기

- 버튼은 새 탭에서 네이버 지도 길찾기 URL을 연다.
- 출발지는 사용자 현재 위치(허용 시), 목적지는 저장된 위경도·상호명이다.
- 수용 기준: 모바일 네이버 지도 앱 설치 여부와 무관하게 웹 길찾기 fallback이 열린다.

### C. 직전 회차 결과

- 번호는 색만으로 구분하지 않고 숫자를 크게 표시.
- 회차/추첨일/1~5등 당첨자 수/총 당첨금 기준시각을 함께 둔다.
- 수용 기준: 원천 URL과 수집 시각이 없으면 결과를 게시하지 않는다.

### D. 기간별 번호 통계

- 기간 탭: 1개월, 3개월, 6개월, 1년, 5년.
- 각 번호에 출현 횟수와 해당 기간 평균 대비 차이만 표시.
- ‘추천 번호’처럼 보이는 색상·정렬 금지; ‘많이 나온 번호’와 ‘다음 추첨 확률’은 다르다고 고지.

### E. 최다 당첨 이력

- 기본은 오프라인 1등 이력 많은 지점 순.
- 1/2/3/4/5등 필터와 지역 필터 제공.
- 4·5등 이력이 데이터 신뢰도 또는 양이 낮으면 기본 탭에서 숨기고 필터로만 연다.

### F. 오류 제보

- 선택: 폐점 / 이전 / 주소 오류 / 지도 위치 오류 / 기타
- v1에는 텍스트만 받고, 이메일은 선택 입력.
- rate limit + honeypot 적용, 운영자는 pending/approved/rejected로 처리.

### G. 후원형 번호 추천 (v1.1 이후)

- 첫 버전은 후원 결제 없이 무료 ‘무작위 5조합’으로 UX와 오인 위험을 검증한다.
- 결제 도입은 후원 페이지와 추천 결과 페이지를 분리한다.
- 데이터 기반 문구 대신 **‘통계 보드에서 선택한 조건을 반영한 오락용 조합’**으로 표현한다.
- 결제 전 법률/PG/플랫폼 정책 확인이 통과하지 않으면 이 기능을 출시하지 않는다.

---

## 7. 기술 스택 제안

| 영역 | 선택 | 이유 |
|---|---|---|
| 웹/PWA | Next.js + TypeScript | 모바일 웹·SEO·Vercel 배포 |
| 지도 | Naver Maps JavaScript API | 국내 주소/길찾기 사용성 |
| DB | Supabase Postgres + PostGIS | 좌표·거리 검색·관리 화면 |
| 배치 | GitHub Actions 또는 Vercel Cron | 회차/판매점 갱신 |
| 지오코딩 | Naver Cloud Platform Geocoding | 주소→좌표, 결과 캐시 필수 |
| 분석 | Python 정규화/집계 scripts | 원천 CSV 검증과 재현성 |
| 오류 제보 | Supabase table + 서버 API | v1 관리 비용 최소화 |
| 결제 | v1 제외; v1.1 Toss Payments 후보 | 정책 확인 전 연결 금지 |

---

## 8. 구현 순서

### Phase 0 — 데이터 생존성 검증

1. 새 저장소 `lotto-map` 생성, legacy 코드 없이 data import만 복사
2. `winning_stores.json`의 주소 정규화 규칙 작성
3. 상위 200개 1·2등 이력 판매점만 Naver 지오코딩으로 샘플 검증
4. 좌표 성공률, 중복률, API 비용, 길찾기 링크를 확인
5. 성공률 90% 미만이면 전체 지오코딩/서비스 제작을 중단하고 주소 정제부터 수정

### Phase 1 — 지도 MVP

1. DB migration: `stores`, `winning_store_records`, `store_reports`
2. data import command와 지오코딩 job 작성
3. 지도 홈 + 위치 권한 + 5km 목록
4. 등수 이력 핀/클러스터 + 판매점 바텀시트
5. Naver 길찾기 deep link
6. 오류 제보 API와 관리자 pending 목록
7. 실제 휴대폰 360px, 390px, 430px에서 테스트

### Phase 2 — 재방문 기능

1. `draw_results` 원천 수집/검증 job
2. 직전 회차 화면
3. 기간별 번호 통계 집계 API
4. 최다 당첨 이력 탭·지역 필터
5. 주간 데이터 갱신 cron과 실패 알림

### Phase 3 — 수익화 판단

1. 광고 없이 DAU·길찾기 전환·재방문율부터 측정
2. 추천 번호 무료 UX의 오해/신고 비율 확인
3. 법률·PG 정책 통과 시에만 후원 페이지 분리 구현
4. 그렇지 않으면 지도 상단의 비침해형 광고 또는 지역 제휴를 먼저 실험

---

## 9. 성공 지표와 중단 기준

| 지표 | 초기 목표 | 실패 신호 |
|---|---:|---|
| 지도 로딩 성공률 | 99%+ | API 키/지도 오류 1% 이상 |
| 상위 판매점 지오코딩 성공률 | 90%+ | 주소 정제 없이 전체 확장 금지 |
| 길찾기 전환율 | 지도 상세 방문의 10%+ | 3% 미만이면 목록/CTA 재설계 |
| 오류 제보 처리 SLA | 7일 이내 | pending 누적 30건 이상 |
| 4주 재방문율 | 15%+ | 5% 미만이면 회차/통계 재방문 가치 재검토 |
| ‘명당/확률’ 오해 제보 | 매우 낮음 | 표현·핀 라벨 즉시 수정 |

---

## 10. 출시 전 체크리스트

- [ ] Naver Maps/Geocoding 사용량·과금·표시 의무 확인
- [ ] 동행복권 원천 데이터의 사용 범위·출처 표시 확인
- [ ] 좌표 없는 14,079개 판매점에 대해 실제 지오코딩 성공률 측정
- [ ] 길찾기 URL을 iOS/Android Chrome·Naver app에서 확인
- [ ] 위치정보 수집/사용 동의와 개인정보처리방침 작성
- [ ] 제보 입력 API rate limit, spam 방지, 보관 기간 고지
- [ ] 로또 통계/후원 기능의 사행성 오인 문구 제거
- [ ] 모바일 실제 기기 360/390/430/768px 시각·터치 테스트

---

## 결론

이 기획은 ‘로또 예측 앱’이 아니라 **신뢰 가능한 판매점 탐색과 회차 정보 서비스**로 시작할 때 성립한다. 첫 개발은 지도보다 먼저 `주소 → 좌표` 성공률을 검증하는 작업이며, 번호 추천 유료화는 트래픽과 정책 검토 뒤에만 넣는다.
